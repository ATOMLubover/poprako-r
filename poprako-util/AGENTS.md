# Shared utilities

- This crate provides localization and time conversion. Do not introduce
  application-domain dependencies.
- Verify changes with `cargo test -p poprako-util` in addition to the root
  Rust formatting and compilation checks.
